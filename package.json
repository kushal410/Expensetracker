{
  "name": "expensetracker-root",
  "private": true,
  "workspaces": [
    "my-expense-tracker"
  ],
  "scripts": {
    "dev": "npm --prefix my-expense-tracker run dev",
    "build": "npm --prefix my-expense-tracker run build",
    "start": "npm --prefix my-expense-tracker run start",
    "lint": "npm --prefix my-expense-tracker run lint"
  }
}
